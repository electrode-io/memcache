/* eslint-disable */

var itemSize = 2000;
var totalItems = 50000;
var iterations = 100;
var sliceSize = Math.ceil(itemSize * 0.8);

module.exports = {
  itemSize,
  totalItems,
  iterations,
  sliceSize
};
