language: node_js
node_js:
  - v4
  - v6
notifications:
  email:
    - joel123@gmail.com
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
before_install: if [[ `npm -v` != 3* ]]; then npm i -g npm@3; npm --version; fi
